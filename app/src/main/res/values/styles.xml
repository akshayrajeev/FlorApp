<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorLeafGreen</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SwitchTheme" parent="Theme.AppCompat">
        <item name="android:colorForeground">@color/colorWhite</item>
        <item name="colorSwitchThumbNormal">@color/colorWhite</item>
        <item name="colorControlActivated">@color/colorLeafGreen</item>
    </style>

</resources>
